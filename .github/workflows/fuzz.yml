name: Fuzz Testing

# Budget: exactly 4 hours (240 min) total compute per day.
#
# build job:  ~10 min  (1 runner)
# 10 fuzz jobs in parallel: ~23 min each (1200s fuzz + overhead)
#             = ~230 min (10 runners)
# Total       = ~240 min = 4 hours
#
# Runs once per day on schedule only, no push/PR triggers.

on:
  schedule:
    - cron: '0 4 * * *'  # Daily at 4am UTC

concurrency:
  group: fuzz
  cancel-in-progress: true

jobs:
  build-fuzz:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4

    - name: Build fuzzing container
      run: docker build . --file ci/docker/Dockerfile.fuzz --tag opensmtpd:fuzz

    - name: Save Docker image
      run: docker save opensmtpd:fuzz | gzip > /tmp/fuzz-image.tar.gz

    - name: Upload Docker image
      uses: actions/upload-artifact@v4
      with:
        name: fuzz-image
        path: /tmp/fuzz-image.tar.gz
        retention-days: 1

  fuzz:
    needs: build-fuzz
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        target:
          - fuzz_rfc5322
          - fuzz_mailaddr
          - fuzz_dns_unpack
          - fuzz_envelope
          - fuzz_netaddr
          - fuzz_relayhost
          - fuzz_credentials
          - fuzz_userinfo
          - fuzz_expandnode
          - fuzz_validation

    steps:
    - name: Download Docker image
      uses: actions/download-artifact@v4
      with:
        name: fuzz-image

    - name: Load Docker image
      run: gunzip -c fuzz-image.tar.gz | docker load

    - name: Run ${{ matrix.target }}
      run: |
        mkdir -p artifacts
        corpus_name=$(echo "${{ matrix.target }}" | sed 's/^fuzz_//')
        docker run --rm \
          -v "$(pwd)/artifacts:/artifacts" \
          opensmtpd:fuzz \
          sh -c "
            mkdir -p /artifacts/${{ matrix.target }}
            /opensmtpd/fuzz/build/${{ matrix.target }} \
              -max_total_time=1200 \
              -artifact_prefix=/artifacts/${{ matrix.target }}/ \
              -print_final_stats=1 \
              /opensmtpd/fuzz/corpus/${corpus_name}/ \
              2>&1 || true
          "

    - name: Check for crashes
      run: |
        crashes=$(find artifacts/${{ matrix.target }} -name 'crash-*' -o -name 'leak-*' -o -name 'timeout-*' 2>/dev/null | head -20)
        if [ -n "$crashes" ]; then
          echo "::error::Fuzzer found issues in ${{ matrix.target }}:"
          echo "$crashes"
          exit 1
        fi

    - name: Upload crash artifacts
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: fuzz-crashes-${{ matrix.target }}
        path: artifacts/${{ matrix.target }}/
        retention-days: 30
